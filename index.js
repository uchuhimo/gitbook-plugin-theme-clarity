module.exports = {
    hooks: {
        config: function(config) {
            // config.styles = config.styles || config.pluginsConfig['theme-clarity'].styles;

            return config;
        }
    }
};
